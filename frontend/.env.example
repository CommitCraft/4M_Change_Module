# Frontend API base URL
VITE_API_BASE_URL=http://localhost:5000/api

# Dev proxy target for Vite server (/api -> target/api)
# Set this to your backend LAN URL when frontend and backend are on different machines.
VITE_DEV_API_PROXY_TARGET=http://localhost:5000
